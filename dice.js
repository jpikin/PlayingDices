const playingDice = function () {
    const numberOfDices = parseInt(prompt("Please enter the number of dice."));
    const numberOfFaces = prompt("Please enter the number of faces on the dice");

    for (let i = 1; i <= numberOfDices; i++) {
        
    }
}